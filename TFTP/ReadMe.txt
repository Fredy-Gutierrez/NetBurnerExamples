Doxygen Format
/** @pagegroup{pageExamples,TFTP,TFTP Example}

 This example program will use a NetBurner device to communicate
 with the NetBurner TFTP Server running on a PC. To run the example:
    1. The TFTP Server on the PC must be started and configured to
       handle both reads and writes.
    2. You must specify the TFTP server in the NetBurner device using
       IPSetp or through the monitor

*/