Doxygen Format
/** @pagegroup{DHCP,ChangeIP,Change IP}

    Demonstrate how to view IP address and change between DHCP and
    static IPv4 IP settings.

    To change from DHCP to Static: M
    - Modify the static IP settings
    - Set address mode to "Static"
    - save to the configuration flash
    - reboot the device

    To change from Static to DHCP,
    - You can leeave the static settings intact if you wish, it will not affect DHCP
    - Set address mode to "DHCP" or "DHCP w Fallback"
    - save to the configuration flash
    - reboot the device

*/