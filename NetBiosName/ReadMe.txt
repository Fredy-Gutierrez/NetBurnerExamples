Doxygen Format
/** @pagegroup{pageExamples,NetBiosName,NetBiosName}

The example demonstrates how to use NetBIOS naming, so you can
access your NetBurner device by name as well as IP address
on a local area network. The name can be changed on the web page.

The NetBios Name can contain any alphanumeric character except spaces
and \ : / * ? ; | . It must be 15 characters or less.

*/